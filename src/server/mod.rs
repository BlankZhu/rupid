mod backend;
mod entrance;
mod load_balancer;
mod middleware;
mod proxy;

use crate::Config;

pub struct Server {}

impl Server {
    fn new(conf: Config) -> Self {
        todo!()
    }

    async fn run() {
        todo!()
    }
}
